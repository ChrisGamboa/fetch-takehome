export default function PaginationBar({ navOptions }) {
    return (
        <div className="join">
            <button className="join-item btn">«</button>
            <button className="join-item btn">...</button>
            <button className="join-item btn">»</button>
        </div>
    )
}